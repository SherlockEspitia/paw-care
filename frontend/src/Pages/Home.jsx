import { Slider } from '@/components';
import SectionClient from '../components/SectionClient/SectionClient';

const Home =() => {
    return (
        <>
            <Slider />
            <SectionClient />
        </>
    );
}

export default Home;